// I'm using a const in case status name changes in the future.
// This const will be easier to search for vs. if the word "available" is used in the markup + logic.
// If there were a few more consts, I would break them out into a separate file.

export const STATUS_AVAILABLE = "available";
